import { MenuByCategoryPipe } from './menu-by-category.pipe';

describe('MenuByCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new MenuByCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
